import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      padding: 20,
      backgroundColor: '#fff',
    },
    imageBox: {
      width: '100%',
      height: 400,
      borderRadius: 12,
      overflow: 'hidden',
    },
    image: {
      width: '100%',
      height: '100%'
    },
    titleText: {
      fontSize: 24,
      fontWeight: 'bold'
    },
    relaseText: {
      fontSize: 16,
      fontWeight: 'bold'
    }
  
  })